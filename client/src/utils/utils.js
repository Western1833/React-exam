export const PATHS = {
    baseUrl: 'http://localhost:3030',
    home: '/',
    login: '/users/login',
    register: '/users/register',
    logout: '/users/logout',
    cars: '/data/cars',
    create: '/data/create',
}

export const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;